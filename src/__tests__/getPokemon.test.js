import React from 'react'
import { getPokemon } from '../getPokemon'

describe('makes sure our fetch function works properly', () => {
    test('should use the correct url', () => {
        //using the correct url
    })
    test('should be called once for every fetch', () => {
        //tobecalledtimes
    })

    test('should be in the catch block when the fetch fails', () => {})
})